import 'package:flutter/material.dart';

class HotelDelRoomPage extends StatelessWidget {
  const HotelDelRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'HotelDelRoomPage',
      ),
    );
  }
}
