import 'package:flutter/material.dart';

class HotelListPkgPage extends StatelessWidget {
  const HotelListPkgPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'HotelListPkgPage',
      ),
    );
  }
}
