import 'package:flutter/material.dart';

class HotelListRoomPage extends StatelessWidget {
  const HotelListRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'HotelListRoomPage',
      ),
    );
  }
}
